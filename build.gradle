/*
 * This build file was auto generated by running the Gradle 'init' task
 * by 'alein' at '10/30/14 9:48 AM' with Gradle 2.1
 *
 */

// Apply the java plugin to add support for Java
apply plugin: 'java'
apply plugin: 'application'

def jarDirectory = new File('../runJar') 
mainClassName = "is.ru.verk.TicTacToe"

// In this section you declare where to find the dependencies of your project
repositories {
    // Use 'jcenter' for resolving your dependencies.
    // You can declare any Maven/Ivy/file repository here.
    jcenter()
}

// In this section you declare the dependencies for your production and test code
dependencies {
	compile 'org.slf4j:slf4j-api:1.7.5'
	testCompile 'junit:junit:4.11'
	compile 'org.hamcrest:hamcrest-core:1.3'
        compile 'com.sparkjava:spark-core:1.1.1'
}

//Tell the jar 'function' where the main class is so it can create the jar file
jar {
	manifest {
		attributes("Main-Class": "is.ru.verk.TicTacToe")
	 }
}

//task runJar(dependsOn:jar) <<  {
//	javaexec { main="-jar"; args build.libs.TicTacToe } 
//}


task runJarFromAnotherDir {
    doFirst{
        if(!jarDirectory.isDirectory()){
            jarDirectory.mkdirs()
        }
    }
    copy {
        from "build/libs/"
        into '../runJar'
    }   
}

run{
	standardInput = System.in
}

task stage { dependsOn installApp }
